Test 10: PEP8 validation (basic check without subprocess)
>>> MyList = __import__('1-my_list').MyList
>>> import inspect
>>> source = inspect.getsource(MyList.print_sorted)
>>> "import" not in source  # Ensure no modules are imported
True
>>> "def print_sorted" in source  # Ensure method exists
True
